# bebewa-backend

```sql
DROP TABLE IF EXISTS
    user_profiles, admins, drivers, addresses, merchants, recipients,
    models, makes, vehicles, vehicle_images, commodities, commodity_images,
    orders, reviews, alembic_version
CASCADE;
```

```sql
DROP TYPE IF EXISTS admin_status_enum;
```
